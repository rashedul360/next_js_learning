import React from "react";

const page = () => {
 return (
  <div>
   {/* link --> http://localhost:3000/about/contact  */}
   <h3>{`(marketing ) then about -> (shop) -> contact page `}</h3>
  </div>
 );
};

export default page;
